<div class="col-lg-4 search_row">
  <div style="padding: 0">
    <div class="col-lg-12"><%=AMERICA_NEW_YORK_TIME_ZONE.format_date(video.date)%></div>
    <h4 class="col-lg-12">
    <% if @current_user.admin? %>
      <%=link_to video.title.truncate(31), edit_statement_path(video), title: video.title  %>
    <% else %>
      <div title="<%=video.title%>"><%=video.title.truncate(31) %></div>
    <% end %>
    </h4>
    <iframe class="col-lg-12 ifrmq" src="<%=video.youtube_embed_url%>?hd=1&rel=0&autohide=1&showinfo=0" frameborder="0"></iframe>
  </div>
</div>